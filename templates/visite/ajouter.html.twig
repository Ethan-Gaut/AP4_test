{% extends 'base.html.twig' %}

{% block title %}Ajouter une visite{% endblock %}

{% block body %}
    <h1>Ajouter une visite</h1>
    {{ form_start(form) }}
    {{ form_row(form.datePrevue) }}
    {{ form_row(form.duree) }}
    {{ form_row(form.compteRenduInfirmiere) }}
    {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}
