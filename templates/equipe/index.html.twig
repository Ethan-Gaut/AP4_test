{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <div class="example-wrapper">
        <h1>Equipe ✅</h1>

        {% for personne in personnes %}
            <div class="card">

                <div class="card-body">
                    <h5 class="card-title">{{ personne.nom }} {{ personne.prenom }}</h5>
                </div>
            </div>
        {% endfor %}
    </div>
    {#
    {% for inspiration in inspirations %}
        {% if app.request.locale == "fr" and inspiration.franceActive == true %}
            <div class="mt-4 col-sm-12 col-md-6  col-lg-6 col-xl-4 col-12 px-md-6" id="cards">
                <div class="card" style="width: 20rem;">
                    <img src="{{ urlImageEnTete ~ inspiration.url }}" class="card-img-top " style="height:300px; width:auto;" alt="...">
                    <div class="card-body cards-inspiration-body">
                        <h5 class="card-title text-center vignette-title-I" style="font-weight: normal !important; font-size: 20px !important;">{{ inspiration.translate.titre }}</h5>
                        <div class="descriptif-inspirations" style="height: 150px;">{{ inspiration.translate.descriptif|striptags|raw|slice(0, 130) }} ...</div>
                        <a href="{{ path('localized_inspiration', {seoUrl: inspiration.translate.seoUrl}) }}" class="btnsuite">Lire la suite</a>
                        <span class="tagsgauche">
                            {% for tag in inspiration.tags %}
                                {% if loop.index > 1 %} | {% endif %} {{tag.translate.titre}}
                            {% endfor %}
                        </span>
                    </div>
                </div>
            </div>
        {% endif %}
    {% endfor %}
    #}

</div>
{% endblock %}
